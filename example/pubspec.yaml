name: example
description: An example app for reactive_multi_image_picker
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.14.0-0 <3.0.0"
  flutter: ">=2.5.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  reactive_forms: ^10.6.0
  freezed_annotation: ^0.14.3
  cross_file: ^0.3.1+4
  reactive_multi_image_picker:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  build_runner: ^2.1.1
  freezed: ^0.14.5

flutter:
  uses-material-design: true
